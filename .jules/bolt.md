# Bolt's Journal

## 2024-05-22 - DateFormatter Performance
**Learning:** `Date().formatted()` creates a new formatter instance every time.
**Action:** Use `static let` formatters in Models/ViewUtils for lists.
